
const modalsWidth = {
    settleUpForm: '37.5rem',
} as const

const sidebarsWidth = {
    expenseForm: '30rem',
} as const

const overlays = { modalsWidth, sidebarsWidth } as const

export default overlays;
